/**
 * AUTH LAYOUT
 * Applied to: /login (admin login page)
 * Bare layout — no Navbar, no Footer, no Chatbot.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
